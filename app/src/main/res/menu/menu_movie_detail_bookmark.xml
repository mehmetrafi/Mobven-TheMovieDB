<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context="com.mobven.moviedb.activity.MovieDetailActivity">
    <item
        android:id="@+id/action_bookmarked"
        android:icon="@drawable/ic_action_bookmarked"
        android:title="@string/action_bookmarked"
        android:visible="false"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/action_bookmark"
        android:icon="@drawable/ic_action_bookmark"
        android:title="@string/action_bookmark"
        android:visible="false"
        app:showAsAction="ifRoom"/>
</menu>
